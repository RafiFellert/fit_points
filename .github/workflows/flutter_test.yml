name: Flutter Unit Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'  # Update to your Flutter version

      - name: Install dependencies
        run: flutter pub get
        #working-directory: GymApp1/gym_app_flutter
        working-directory: GymApp1/GymApp1/gym_app_flutter

      - name: Run unit tests
        run: flutter test test/exercise_counter_test.dart --coverage
        working-directory: GymApp1/GymApp1/gym_app_flutter

      - name: Upload coverage to GitHub Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: coverage-report
          path: coverage/lcov.info